"""
Conftest for SDK compatibility tests.

Minimal setup — all fixtures and helpers are in the test file itself to avoid
import issues. This conftest only ensures the test directory is recognized.
"""
